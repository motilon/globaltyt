# Impedir que se pueda ver el listado de contenidos de un directorio

Options All -Indexes

# Url Amigables

RewriteEngine On

RewriteRule ^formatos/(.*) formato.php?id=$1
RewriteRule ^formatos formato.php?id=0